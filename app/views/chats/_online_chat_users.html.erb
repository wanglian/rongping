<%- online_chat_users.each do |chat_user| -%>
  <%= user_link chat_user.user %><% if chat_user.state == 'requesting' %>(<%= "request to join"[] %> <%= link_to_remote 'approve'[], :url => accept_chatroom_path(@chatroom, :user_id => chat_user.user_id), :method => :put %>)<% end %>
<%- end -%>